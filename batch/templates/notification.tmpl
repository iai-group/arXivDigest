<!DOCTYPE html>
<html>

<head>
    <style>
        html {
            font-family: 'Trebuchet MS'
        }

        .centerDiv {
            margin: 0 auto;
            width: 800px;
            background-color: white;
            border: none;
            border-radius: 10px;
            padding: 5px;
        }

        .text {
            text-align: center;
            color: rgb(59, 59, 59);
        }

        .authors {
            color: rgb(185, 185, 185);
            font-size: 0.9em;
        }

        h3 {
            margin-bottom: 2px;
        }

        ul {
            list-style: none;
        }
    </style>
</head>

<body style="background-color:#dddddd">
    <div class="centerDiv">
        <div class="text">
            <h2>Hi {{name}}</h2>
            <p>Here are personalised suggestions for articles to read based on your ARX profile:</p>
        </div>
        <hr>
        <div class="content text">
            {%for article in articles%}
            <a href="{{article.readlink}}">
                <h3>{{article.title}}</h3>
            </a>
            <span class="authors">{{ article.authors }}</span>
            <a href={{article.likelink}}>
                <p>Like</p>
            </a>
            <br> {%endfor%}
        </div>
        <hr>
        <div class="text">
            <p>You are receiving this email because you have registered with ARX litterature reccomender</p>
            <p>Go to our webpage for more:</p>
            <a href="#">
                <p>ARX reccomender</p>
            </a>
        </div>
    </div>
</body>

</html>